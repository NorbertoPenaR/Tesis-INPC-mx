# DVAE (Conditional VAE) for Time Series Forecasting — PyTorch (Python 3.12)

Minimal CVAE for forecasting with GRUs (encoder/decoder). Ready for conda + CPU.
See `config_example.yaml` and `README.md` for usage.
